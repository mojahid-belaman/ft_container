#ifndef VECTOR_HPP
#define VECTOR_HPP

#include <iostream>
#include <deque>
#include <stack>

namespace ft
{
    template <class T, class Alloc = std::allocator<T> >
    class vector
    {
        private:
            
        public:
            void    push_back(const T&);

    };
}

#endif